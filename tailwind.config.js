/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
      fontFamily: {
        calibri: ["Calibri"], // Define a fonte Calibri como padrão
      },
    },
  },
  plugins: [],
};
